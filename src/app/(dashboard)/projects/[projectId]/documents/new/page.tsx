import Link from "next/link"; import { ChevronLeft } from "lucide-react"; import { requirePageUser } from "@/lib/auth"; import { listDocuments } from "@/features/documents/service"; import { DocumentEditor } from "@/components/documents/document-editor";
export default async function NewDocumentPage({params}:{params:Promise<{projectId:string}>}){const {projectId}=await params;const actor=await requirePageUser();const rights=await listDocuments(projectId,actor,{page:1,pageSize:1});if(!rights.permissions.canEdit)return <p>Bạn không có quyền tạo tài liệu.</p>;return <><Link href={`/projects/${projectId}/documents`} className="inline-flex items-center gap-1 text-sm text-slate-500"><ChevronLeft className="size-4"/>Tài liệu</Link><h1 className="my-6 text-3xl font-semibold">Tạo tài liệu</h1><DocumentEditor projectId={projectId}/></>}
