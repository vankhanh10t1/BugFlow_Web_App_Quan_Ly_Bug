import Link from "next/link"; import { ChevronLeft } from "lucide-react"; import { requirePageUser } from "@/lib/auth"; import { getDocument, getRevision } from "@/features/documents/service"; import { MarkdownViewer } from "@/components/documents/markdown-viewer"; import { RestoreRevisionButton } from "@/components/documents/document-actions";
export default async function RevisionPage({params}:{params:Promise<{projectId:string;documentId:string;revisionId:string}>}){const p=await params;const actor=await requirePageUser();const [doc,revision]=await Promise.all([getDocument(p.projectId,p.documentId,actor),getRevision(p.projectId,p.documentId,p.revisionId,actor)]);return <><Link href={`/projects/${p.projectId}/documents/${p.documentId}/revisions`} className="inline-flex items-center gap-1 text-sm text-slate-500"><ChevronLeft className="size-4"/>Lịch sử</Link><div className="my-6 flex flex-wrap items-start justify-between gap-4"><div><p className="text-sm font-medium text-blue-700">Phiên bản {revision.version}</p><h1 className="mt-2 text-3xl font-semibold">{revision.title}</h1><p className="mt-2 text-sm text-slate-500">{revision.createdBy.fullName} · {new Intl.DateTimeFormat("vi-VN",{dateStyle:"medium",timeStyle:"short"}).format(revision.createdAt)}</p></div>{doc.permissions.canManage?<RestoreRevisionButton projectId={p.projectId} documentId={p.documentId} revisionId={p.revisionId}/>:null}</div><article className="rounded-xl border bg-white p-6 sm:p-8"><MarkdownViewer content={revision.content}/></article></>}
