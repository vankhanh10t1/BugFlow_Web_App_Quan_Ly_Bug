import Link from "next/link";
import { ShieldCheck } from "lucide-react";
import { requirePageUser } from "@/lib/auth";
import { getTwoFactorStatus } from "@/features/auth/two-factor-service";
import { TwoFactorManagement } from "@/components/auth/two-factor-settings";
export const metadata = { title: "Cài đặt bảo mật" };
export default async function SecuritySettingsPage() { const user = await requirePageUser(); const status = await getTwoFactorStatus(user.id); return <div><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-sm font-medium text-blue-600">Tài khoản</p><h1 className="mt-1 text-3xl font-semibold tracking-tight">Cài đặt bảo mật</h1><p className="mt-2 text-slate-600">Bảo vệ tài khoản bằng ứng dụng Authenticator và recovery code.</p></div><span className={`inline-flex items-center gap-2 rounded-full px-3 py-1.5 text-sm font-medium ${status.enabled ? "bg-emerald-50 text-emerald-700" : "bg-slate-200 text-slate-600"}`}><ShieldCheck className="size-4" />{status.enabled ? "2FA đang bật" : "2FA đang tắt"}</span></div>{status.enabled ? <><div className="my-8 rounded-xl border bg-white p-5 text-sm text-slate-600"><p>Bật từ: <strong>{status.enabledAt?.toLocaleString("vi-VN")}</strong></p><p className="mt-1">Recovery code chưa dùng: <strong>{status.remainingRecoveryCodes}</strong></p></div><TwoFactorManagement /></> : <section className="mt-8 rounded-xl border bg-white p-6"><h2 className="font-semibold">Two-factor authentication chưa được bật</h2><p className="mt-2 text-sm leading-6 text-slate-600">Sau khi bật, mật khẩu đúng vẫn chưa đủ để tạo session. Bạn phải xác minh thêm mã TOTP.</p><Link href="/settings/security/2fa/setup" className="mt-5 inline-flex h-10 items-center rounded-lg bg-blue-600 px-4 text-sm font-medium text-white">Thiết lập 2FA</Link></section>}</div>; }
