import { apiError, apiSuccess } from "@/lib/api-response"; import { requireActiveUser } from "@/lib/auth"; import { assertSameOriginRequest } from "@/lib/request-security"; import { enforceUserMutationLimit } from "@/lib/rate-limit"; import { restoreRevision } from "@/features/documents/service";
type Context = { params: Promise<{ projectId: string; documentId: string; revisionId: string }> };
export async function POST(request: Request, { params }: Context) { try { assertSameOriginRequest(request); const actor = await requireActiveUser(); await enforceUserMutationLimit("document:restore", actor.id, 10); const p = await params; return apiSuccess(await restoreRevision(p.projectId, p.documentId, p.revisionId, actor), "Đã khôi phục phiên bản"); } catch (error) { return apiError(error); } }
