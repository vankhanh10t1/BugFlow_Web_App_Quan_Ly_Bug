type Activity = { id: string; actionType: string; description: string; oldValue: string | null; newValue: string | null; createdAt: Date; actor: { fullName: string } };
export function ActivityTimeline({ activities }: { activities: Activity[] }) { return <section className="mt-12"><h2 className="text-xl font-semibold">Hoạt động</h2><ol className="mt-5 rounded-xl border bg-white px-5">{activities.length ? activities.map((item) => <li key={item.id} className="relative border-l py-4 pl-6"><span className="absolute -left-1.5 top-6 size-3 rounded-full border-2 border-white bg-blue-500" /><p className="text-sm"><span className="font-medium">{item.actor.fullName}</span> {item.description.toLowerCase()}</p>{item.oldValue || item.newValue ? <p className="mt-1 text-xs text-slate-500">{item.oldValue ?? "—"} → {item.newValue ?? "—"}</p> : null}<time className="mt-1 block text-xs text-slate-400">{new Date(item.createdAt).toLocaleString("vi-VN")}</time></li>) : <li className="py-8 text-center text-sm text-slate-500">Chưa ghi nhận hoạt động nào.</li>}</ol></section>; }
