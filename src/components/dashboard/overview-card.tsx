import type { LucideIcon } from "lucide-react";
export function OverviewCard({ icon: Icon, label, value, note, tone = "blue" }: { icon: LucideIcon; label: string; value: string | number; note: string; tone?: "blue" | "red" | "amber" | "emerald" }) { const colors = { blue: "bg-blue-50 text-blue-600", red: "bg-red-50 text-red-600", amber: "bg-amber-50 text-amber-600", emerald: "bg-emerald-50 text-emerald-600" }; return <article className="rounded-xl border bg-white p-5 shadow-sm"><span className={`grid size-10 place-items-center rounded-lg ${colors[tone]}`}><Icon className="size-5" /></span><p className="mt-4 text-sm text-slate-500">{label}</p><p className="mt-1 text-2xl font-semibold tabular-nums">{value}</p><p className="mt-2 text-xs text-slate-400">{note}</p></article>; }
